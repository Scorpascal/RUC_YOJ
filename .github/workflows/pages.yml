name: Deploy RUC YOJ learning site and quick-submit helper

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "data/problems.json"
      - "data/quick-submit.json"
      - "data/public-ready.json"
      - "data/yoj-public-problems.json"
      - "tools/build_site_catalog.py"
      - "tools/build_initial.py"
      - "tools/audit_consistency.py"
      - "tools/audit_online_availability.py"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Configure Pages
        uses: actions/configure-pages@v6
      - name: Verify generated Pages data
        run: python3 tools/build_site_catalog.py --check
      - name: Verify traffic data invariants
        run: python3 tools/build_traffic.py --check
      - name: Audit public release evidence
        run: python3 tools/audit_consistency.py
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
