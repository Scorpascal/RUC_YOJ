#include <stdio.h>
#include <math.h>

____qcodep____

int main() {
    struct Point p1, p2;
    
    // 输入两个点的坐标
    scanf("%lf %lf", &p1.x, &p1.y);
    scanf("%lf %lf", &p2.x, &p2.y);

    ____qcodep____

    return 0;
}
