# 1680. 蒙特卡洛（CSP 202509-1）

> 当前文件由 YOJ 原始题面快照的 `p_content` 离线转换生成。公式尽量转换为 LaTeX，图片保留为 Markdown 图片链接。
> 当前仓库阶段：`RAW_CAPTURED`。代码尚未完成脱敏清理、版本规范化、提交形态核验和在线复验。

## 题目信息

- 原题地址：[http://yoj.ruc.edu.cn/index.php/index/problem/detail/pno/1680.html](http://yoj.ruc.edu.cn/index.php/index/problem/detail/pno/1680.html)
- 时间限制：143 ms
- 内存限制：504 KB
- 归档语言：`cpp`

---

# 蒙特卡洛

时间限制 1 秒，空间限制 512 MiB。

### 题目描述

给定正整数 a，在平面直角坐标系中画一个边长为 2a 的正方形，左下角和右上角分别为 (-a,-a)、(a,a)，再画一个以原点为圆心、a 为半径的圆。在正方形内（含边界）随机生成 n 个坐标点，统计其中落在圆内（含边界）的点数 m。圆和正方形的面积比约等于 m/n，故用 4m/n 估计 π。给定这些坐标，请输出该估计值。

### 输入格式

第一行两个正整数 n、a。接下来 n 行，每行两个数 x、y，表示一个坐标点。

### 输出格式

输出一个实数，即 4m/n。相对误差或绝对误差小于 0.0001 即可，题面推荐保留六位小数。

### 完整范围与子任务

- 50% 测试点：所有输入坐标均为整数。
- 全部测试点：0<n,a≤1000，坐标在正方形内，坐标最多两位小数。

### 样例解释与测试边界

样例 1 只有 (-5,5) 在圆外，结果为 3.0。样例 2 有三个点在圆内，结果为 2.4。边界包括圆心、圆周、正方形顶点、重复点、全部在圆内/外、n=a=1、两位小数的近圆周点。圆周必须计入。

## 样例 1

输入：

```
4 53 4-5 50 03 4
```

输出：

```
3.0
```

## 样例 2

输入：

```
5 53.01 3.99-5 50 02.9 4.1-3 4
```

输出：

```
2.4
```

---

## 归档状态

- 题面转换：`CONVERTED_FROM_HTML_SNAPSHOT`
- 代码状态：`RAW_CAPTURED`（不可视为已清洗的公开题解）
- 在线 AC 复验：`NOT_RUN`
- 直接提交分块：`UNKNOWN_UNTIL_FORM_MAP`
