# 0260. 二分查找

> 当前文件由 YOJ 原始题面快照的题面主体离线转换生成。公式尽量转换为 LaTeX，图片优先本地化为 Markdown 图片链接；下载失败时保留原始链接并记录 warning。
> 当前仓库阶段：`RAW_CAPTURED`。代码尚未完成脱敏清理、版本规范化、提交形态核验和在线复验。

## 题目信息

- 原题地址：[http://yoj.ruc.edu.cn/index.php/index/problem/detail/pno/260.html](http://yoj.ruc.edu.cn/index.php/index/problem/detail/pno/260.html)
- 题目时间限制（题面）：1000 ms
- 题目内存限制（题面）：256MiB
- 归档语言：`c`
- 本次 AC 实际耗时（提交详情）：69 ms
- 本次 AC 实际内存占用（提交详情）：508 KB

---

在集合中查找某个特定元素是编程中经常遇到的操作。在一个有序的集合中查找元素，可以使用二分查找算法。二分查找法充分利用了元素间的次序关系，是一种效率较好的查找算法。

二分查找的基本思想是：对于含有n个元素的一个有序集合a，将n个元素分成个数大致相同的两半，取a[n/2]与欲查找的x作比较，结果有三种情况：
1. 如果x = a[n/2]，则找到x，算法终止。
2．如果x < a[n/2]，则只要在数组a的左半部继续搜索x（这里假设数组元素呈升序排列）。
3．如果x > a[n/2]，则我们只要在数组a的右半部继续搜索x。

例如9个元素的集合，a[(n-1)/2]将原来的集合分成3部分，

如果x=a[(n-1)/2]则找到，算法结束；

如果x < a[(n-1)/2]，则只要在左边的集合中继续搜索，直到找到某个元素或者查找的集合区间元素个数为0；

如果x > a[(n-1)/2]，则只要右边的集合中继续搜索，直到找到某个元素或者查找的集合区间元素个数为0。

请你写程序实现二分查找算法。
给定一个集合和一个待查找元素，请你先将集合中的元素从小到大排序，接着用二分查找算法在排序后的集合中查找元素。

**输入格式**

输入共3行。
　　第一行包含一个正整数n（1≤n≤1000），表示集合的元素个数。
　　第二行包含一个非负整数m（0≤m≤2^30），表示待查找的整数。
　　第三行包含n个正整数（≤2^30），每两个整数之间用一个空格隔开，是给定集合中的n个元素，输入数据保证这n个整数互不相等。

**输出格式**

输出共两行。
　　第一行包含一个整数k，表示待查找的数在排序后的集合中的位置（位置从0 ~ n-1标号），如果没有找到则输出-1。
　　第二行包含一个整数,表示查找成功前的比较次数。

**输入样例**

10
24
42 24 10 29 27 12 58 31 8 16

**输出样例**

4
1

**特殊提示**

【样例1说明】
　　排序后的集合a为：8 10 12 16 24 27 29 31 42 58
　　待查找的数24，第一次与a[4]比较，a[4]是24，找到元素，算法结束，所以比较次数为1。
　　【样例输入2】
　　10
　　30
　　42 24 10 29 27 12 58 31 8 16
　　【样例输出2】
　　-1
　　4
　　【样例1说明】
　　排序后的集合a为：8 10 12 16 24 27 29 31 42 58
　　待查找的数30，第一次30与a[4]比较，a[4]是24, 24 < 30，所以去集合的后半部分即a[5] ~ a[9]之间搜索；第二次30与a[(5+9)/2]即a[7]比较，a[7]是31，31 > 30，所以要在a[5]~a[6]之间搜索；第三次30与a[(5+6)/2]比较，a[5]是27，27 < 30，所以要在a[6]~a[6]右边查找；第四次30和a[6]比较，a[6]是29，29 < 30，所以在a[6]右边查找，右边的待查找集合为空，所以算法结束，没有找到30，这一过程中比较次数为4。

---

## 归档状态

- 题面转换：`CONVERTED_FROM_HTML_SNAPSHOT`
- 代码状态：`RAW_CAPTURED`（不可视为已清洗的公开题解）
- 在线 AC 复验：`NOT_RUN`
- 直接提交分块：`UNKNOWN_UNTIL_FORM_MAP`
